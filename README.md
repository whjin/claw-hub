# claw-hub

┌─────────────────┐
│ Web UI 交互层 │ ← 前端聊天界面
└────────┬────────┘
│ WebSocket通信
┌────────┴────────┐
│ Gateway 网关层 │ ← 调度中心，处理消息路由、会话管理
└────────┬────────┘
│
┌────────┴────────┐
│ Agent 引擎层 │ ← 核心推理循环，ReAct思考-执行闭环
├─────────────────┤
│ Skills 技能层 │ ← 可扩展的工具能力，文件/搜索/命令执行
├─────────────────┤
│ Memory 记忆层 │ ← 本地会话持久化，保存对话历史
└─────────────────┘
